generator client {
  provider = "prisma-client"
  output   = "../generated/prisma"
}

datasource db {
  provider = "postgresql"
}

model Statistic {
  id   Int @id @default(autoincrement())
  size Int @unique
  X    Int @default(0)
  O    Int @default(0)
  draw Int @default(0)
}
